export class InfoSocialAddress {
    via_type?: string ;
    address?: string;
    number?: number;
    extra?: string;
    social_cp?: number;
    social_municipality?: string;
    social_province?: string ;
    social_region?: string;
    social_country?: string;
    via_region?: string;

    // constructor(via_type: string, address: string, number: number, extra: string,
    //     social_cp: number, social_municipality: string, social_province: string, social_region: string, via_region){

    //     via_type = this.via_type;
    //     address = this.address;
    //     number = this.number;
    //     extra = this.extra;
    //     social_cp = this.social_cp;
    //     social_municipality = this.social_municipality;
    //     social_province = this.social_province;
    //     social_region = this.social_region;
    //     via_region = this.via_region;

    // }
}